<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="475dp"
    android:layout_marginTop="30dp"
    android:layout_marginStart="20dp"
    android:layout_marginEnd="20dp"
    android:layout_gravity="center_horizontal"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@drawable/rounded_background"
    >

    <ImageView
        android:id="@+id/combo_image"
        android:layout_width="wrap_content"
        android:layout_height="300dp"
        android:src="@drawable/kombo3"
        android:paddingHorizontal="20dp"/>

    <TextView
        android:id="@+id/combo_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text = "Комбо от Смешариков"
        android:textStyle="bold"
        android:textSize="26sp"
        android:paddingHorizontal="20dp"
        />

    <TextView
        android:id="@+id/combo_description"
        android:layout_width="360dp"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content"
        tools:text="Одобрено героями мультфильма: маленькая пицца любого вкуса и набор юного садовода. Цена комбо зависит от выбранных пицц и может быть увеличена"
        android:textSize="13sp"
        android:lineSpacingMultiplier="1.2"
        android:textColor="@color/gray"
        android:paddingHorizontal="20dp"/>

    <TextView
        android:id="@+id/combo_price"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        tools:text="от 2 300 KZT"
        android:textColor="@color/text_color"
        android:padding="4dp"
        android:textSize="16sp"
        android:textAlignment="center"
        android:background="@drawable/bg_text"
        android:layout_marginTop="10dp"
        android:layout_marginStart="225dp"
        />

</LinearLayout>